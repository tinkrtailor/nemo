Starting with repo search to identify what `ROUND 20`, `Lane C`, and `CONVERGED if none` map to in this codebase, then I’ll do a read-only pass on the relevant scope.
Prior Lane C rounds found two unresolved issues. I’m verifying those directly against current code and scanning the Lane C surface for any newly introduced regressions before deciding whether this round is clean.
One prior issue may be fixed already: the credentials API now accepts `email`. I’m tracing whether the CLI actually sends it and whether the job builder/runtime still violate the Claude auth and identity contracts.
The Claude auth wiring changed since round 19: the agent now gets a `.claude` mount. I’m checking whether it still violates the spec in a subtler way, especially because the job builder is still exporting `ANTHROPIC_BASE_URL`, which the spec explicitly forbids.
The email path is mostly implemented now. I’m checking for remaining contract violations around actual commit attribution and any other Lane C runtime mismatches that still reproduce on this branch.
1. High: Claude implement/revise auth is still wired against the wrong runtime contract, so Lane C is not converged. The job builder still exports `ANTHROPIC_BASE_URL=http://localhost:9090/anthropic` for agent pods (`control-plane/src/k8s/job_builder.rs:253-256`), and the sidecar still proxies `/anthropic/*` using `/secrets/model-credentials/anthropic` (`images/sidecar/main.go:91-99`, `images/sidecar/main.go:127-155`). But Lane C explicitly requires Claude auth to come directly from the mounted `~/.claude/` session dir and says `ANTHROPIC_BASE_URL` must not be set (`specs/lane-c-agent-runtime.md:40`, `specs/lane-c-agent-runtime.md:49`, `specs/lane-c-agent-runtime.md:55`, `specs/lane-c-agent-runtime.md:77`). `nemo auth` only uploads `claude`, `openai`, and `ssh` (`cli/src/commands/auth.rs:21-34`, `cli/src/commands/auth.rs:126-132`), so if Claude honors `ANTHROPIC_BASE_URL`, implement/revise jobs will look for an `anthropic` secret that is never provisioned. That is still a real production auth failure path.

2. Medium: Git author attribution still does not use the engineer identity contract from config/spec. The CLI/config and API only carry `engineer` plus optional `email`; there is no persisted human `name` field (`cli/src/config.rs:7-15`, `control-plane/src/types/api.rs:143-152`, `control-plane/src/api/handlers.rs:532-544`). The job builder sets `GIT_AUTHOR_NAME`/`GIT_COMMITTER_NAME` from `ctx.engineer` (`control-plane/src/k8s/job_builder.rs:259-263`), and `/credentials` validates that value as a lowercase slug (`control-plane/src/api/handlers.rs:412-421`). But the spec says `nemo auth` should read `[identity].name` and `[identity].email`, upsert the engineer record, and use that identity for git attribution (`specs/lane-b-infrastructure.md:564-569`, `specs/lane-b-infrastructure.md:617`, `specs/lane-c-agent-runtime.md:79`). Email is now propagated, but the actual author name is still the engineer slug, not the configured identity name.

Round 20 result: not `CLEAN`, not `CONVERGED`.
