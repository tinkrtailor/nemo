{
  "name": "egress_http_no_redirect_follow",
  "category": "egress",
  "description": "GET /redirect returns 302; sidecar must NOT follow — client sees 302 and mock observes exactly one request",
  "input": {
    "kind": "http_no_redirect",
    "url": "http://mock-example/redirect"
  },
  "expected_parity": true
}
